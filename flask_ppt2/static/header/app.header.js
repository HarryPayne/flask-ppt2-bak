(function() {
  
  /**
   *  @name app.header
   *  @desc Module for the banner and navigation at the top of all pages (and
   *        not for the <head>). 
   */
  angular
    .module("app.header", ["ui.router"]);
  
}());
